<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron Job: Auto-generate Invoices -->
        <record id="cron_auto_generate_invoices" model="ir.cron">
            <field name="name">Education: Auto-generate Invoices</field>
            <field name="model_id" ref="dar_elanwar.model_education_student_subscription"/>
            <field name="state">code</field>
            <field name="code">model._cron_auto_generate_invoices()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
